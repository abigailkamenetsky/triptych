/*
 * dedication.js
 *
 * ┌──────────────────────────────────────────────────────────────────────┐
 * │  THIS IS THE FILE TO EDIT.                                           │
 * │  Everything below is a placeholder. Replace the words with yours.    │
 * │  Save the file, run `node build.mjs`, and push.                      │
 * │                                                                      │
 * │  Set `lines` to an empty array to skip the card entirely.            │
 * │  No em dashes. Use commas, colons, or a full stop.                   │
 * └──────────────────────────────────────────────────────────────────────┘
 */

export const DEDICATION = {
  /* Blackletter, at the top of the card. Keep it short, it is set very large. */
  heading: 'For my mother',

  /* Each string is its own paragraph. Two or three reads best. */
  lines: [
    'Thank you so much for being my mom. For all the reading you did to me, and all the reading you taught me, and for never once telling me a book was too long.',
    'So here is a library that is only yours. It lives on your iPad, it works with no internet, and nothing in it is watching you read.',
    'Fill it with whatever you like. Happy birthday.',
  ],

  /* Signed, at the foot of the card. Leave as an empty string to omit. */
  signature: 'Abby',

  /* Small italic line under the signature. Empty string to omit. */
  date: 'the first of September, 2026',

  /* The button that opens the shelf. */
  cta: 'Open the garden',
};
